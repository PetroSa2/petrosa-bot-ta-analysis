
aiohttp>=3.8.0
fastapi>=0.110.0

# Technical Analysis
# ta-lib>=0.4.0  # Commented out - requires system dependencies
# Using xgboosted/pandas-ta-classic fork (pandas 2.x compatible, 200+ indicators)
# Original repo (twopirllc/pandas-ta) no longer exists
# Pinned to a specific commit (petrosa_k8s#1052 epic / #274): an unpinned
# `git+https://...` ref floats to whatever the fork's HEAD is at install
# time, which is exactly what turned main red — a later commit changed
# insufficient-data behavior (empty/None result -> all-NaN DataFrame of the
# expected shape), and ta_bot/core/indicators.py's insufficient-data check
# was written for the old shape. Pinning removes that non-determinism; the
# code was additionally hardened to treat all-NaN as insufficient data too,
# so this is defense-in-depth rather than the only fix.
git+https://github.com/xgboosted/pandas-ta-classic.git@9e92da34441f58c248f2d0e1df77a0df7969b56e

# Health checks
healthcheck>=1.3.0
httpx>=0.26.0
motor>=3.3.2
nats-py>=2.0.0
numpy>=2.0.0

# OpenTelemetry Core
opentelemetry-api>=1.20.0

# OpenTelemetry Auto-Instrumentation
opentelemetry-distro>=0.41b0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
opentelemetry-instrumentation>=0.41b0
opentelemetry-instrumentation-aiohttp-client>=0.41b0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-logging>=0.41b0

# OpenTelemetry Manual Instrumentations
opentelemetry-instrumentation-requests>=0.41b0
opentelemetry-instrumentation-urllib3>=0.41b0
opentelemetry-sdk>=1.20.0

# OpenTelemetry Semantic Conventions
opentelemetry-semantic-conventions>=0.41b0
# Core dependencies
pandas>=2.0.0

# Petrosa OTel package
petrosa-otel[all]>=1.1.0
prometheus-client>=0.17.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pymongo>=4.6.0

# Database
pymysql>=1.1.0

# Environment and configuration
python-dotenv>=1.0.0
requests>=2.31.0
starlette>=0.37.0

# Logging and monitoring
structlog>=23.0.0
uvicorn>=0.24.0

# Retry logic for vendored Data Manager client SDK (ta_bot/services/dm_sdk, #267)
tenacity>=8.2.0
